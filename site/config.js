// The one setting the recap page needs: your `recap` function's URL.
// Replace <project-ref> with your Supabase project ref.
window.RECAP_CONFIG = {
  api: "https://<project-ref>.supabase.co/functions/v1/recap",
};
